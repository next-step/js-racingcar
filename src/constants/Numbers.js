export const CAR = Object.freeze({
  START_POSITION: 0,
  RUN_UNIT: 1,
  RUN_THRESHOLD: 4,
  NAME_MIN_LENGTH: 1,
  NAME_MAX_LENGTH: 5,
});

export const RACE = Object.freeze({
  MIN_CAR_COUNT: 2,
  MAX_ROUND: 5,
  MIN_WINNER_COUNT: 1,
  MIN_ROLL_NUMBER: 0,
  MAX_ROLL_NUMBER: 9,
});
