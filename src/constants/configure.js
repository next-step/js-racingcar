export const NAME_CONFIGURE = Object.freeze({
  MIN_LENGTH: 1,
  MAX_LENGTH: 5,
  SEPARATOR: ','
});

export const RACE_CONFIGURE = Object.freeze({
  MAX_SPEED: 9,
  MIN_SPEED: 0,
  MOVE_CONDITION: 4,
  MAX_LAP: 5,
  TRACK: '-',
  DISPLAY_SEPARATOR: ','
});
