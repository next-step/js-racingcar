export const CAR_CONFIGURE = Object.freeze({
  MOVE_CONDITION: 4,
  NAME_MIN_LENGTH: 1,
  NAME_MAX_LENGTH: 5,
  NAME_SEPARATOR: ','
});

export const RACE_CONFIGURE = Object.freeze({
  MAX_SPEED: 9,
  MIN_SPEED: 0,
  TOTAL_LAP: 5,
  TRACK: '-',
  DISPLAY_SEPARATOR: ','
});
