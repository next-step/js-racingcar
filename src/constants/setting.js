const INPUT_SETTING = {
	SPLITER: ",",
	MAX_LENGTH: 5,
	MIN_CARS: 2,
	REGEX: /^[a-zA-Z]*$/,
};

const CAR_SETTING = {
	CAR_NAME: "pobi",
	CAN_MOVE_NUM: 4,
};

export const SETTING = {
	INPUT_SETTING,
	CAR_SETTING,
};
