export const CAR_CONFIGURE = Object.freeze({
  NAME_MIN_LENGTH: 1,
  NAME_MAX_LENGTH: 5,
  MOVE_CONDITION: 4,
  NAME_SEPARATOR: ','
});

export const RACE_CONFIGURE = Object.freeze({
  DISPLAY_SEPARATOR: ',',
  TRACK: '-',
  MAX_LAP: 100,
  MIN_SPEED: 0,
  MAX_SPEED: 9
});
