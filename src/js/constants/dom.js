import {$} from "../utils/index.js";

export const DOM = object.freeze({
  CAR_NAMES_ID_INPUT: $(".input-car-name"),
  CAR_NAMES_SUBMIT_BTN: $(".name-submit-btn"),
  NUMBER_OF_ATTEMPTS_INPUT: $(".number-of-attempts"),
});
