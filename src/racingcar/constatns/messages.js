import { MIN_NAME_LENGTH, MAX_NAME_LENGTH } from '.';

const NOT_ALLOWED_NAME_LENGTH = `유효하지 않은 이름 길이입니다. 자동차의 이름은 ${MIN_NAME_LENGTH}자이상, ${MAX_NAME_LENGTH}자 이하만 가능합니다.`;

export { NOT_ALLOWED_NAME_LENGTH };
